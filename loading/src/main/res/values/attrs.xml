<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="RotateLoading">
        <attr name="loading_width" format="dimension"/>
        <attr name="loading_color" format="color"/>
        <attr name="loading_shadow_position" format="integer"/>
        <attr name="loading_speed" format="integer"/>
    </declare-styleable>

    <declare-styleable name="CradleBall">
        <attr name="cradle_ball_color" format="color" />
    </declare-styleable>

    <declare-styleable name="SpinnerLoading">
        <attr name="circle_color" format="color"/>
        <attr name="circle_color_move" format="color"/>
    </declare-styleable>

    <declare-styleable name="NumberProgress">
        <!-- 文字大小 -->
        <attr name="textSize" format="dimension"/>
        <!-- 文字颜色 -->
        <attr name="textColor" format="color"/>
        <!-- reached的高度 -->
        <attr name="reachedHeight" format="dimension"/>
        <!-- reached的颜色 -->
        <attr name="reachedColor" format="color"/>
        <!-- unReached的高度 -->
        <attr name="unReachedHeight" format="dimension"/>
        <!-- unReached的颜色 -->
        <attr name="unReachedColor" format="color"/>
        <!-- 当前进度 -->
        <attr name="currentProgress" format="integer"/>
        <!-- 总进度 -->
        <attr name="maxProgress" format="integer"/>


        <!-- unProgress颜色 -->
        <attr name="unProgressColor" format="color"/>
        <!-- progress颜色 -->
        <attr name="progressColor" format="color"/>
        <!-- progress宽度 -->
        <attr name="progressWidth" format="dimension"/>
    </declare-styleable>

    <declare-styleable name="ButterFlyLoadView">
        <attr name="figure" format="reference"></attr>
    </declare-styleable>

    <declare-styleable name="FlikerProgressBar">
        <attr name="ftextsize" format="dimension|reference"/>

        <attr name="loadingColor" format="color|reference"/>

        <attr name="stopColor" format="color|reference"/>

    </declare-styleable>
</resources>